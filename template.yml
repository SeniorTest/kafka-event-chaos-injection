parameters:
  param: []
steps:
  - ${{ each p in parameters.param }}:
    - task: PythonScript@0
      displayName: 'Post notebook to SUT'
      inputs:
        scriptSource: inline
        script: |
          import argparse
          import sys
          import traceback
          
          parser = argparse.ArgumentParser()
          parser.add_argument("--testcase_notebook", help="Provide the name of the notebook you want to execute.")
          args = parser.parse_args()
          print ('Running notebook ', args.testcase_notebook)
          print('template instantiated')
      
        arguments: --testcase_notebook ${{ p }}
