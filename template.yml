parameters:
  param: []
steps:
  - ${{ each p in parameters.param }}:
    - task: PythonScript@0
      displayName: 'Post notebook to SUT'
      inputs:
        scriptSource: inline
        script: |
          print('template instantiated')
